# ===========================================
# ПРИМЕР ФАЙЛА .env
# Скопируйте этот файл в .env и заполните своими данными
# ===========================================

# Пароль для доступа к веб-интерфейсу
SITE_PASSWORD=your_password_here

# ===========================================
# НАСТРОЙКИ ПРОКСИ
# ===========================================
# Включить прокси (true/false)
PROXY_ENABLED=false

# Адрес прокси-сервера
PROXY_SERVER=your_proxy_server.com

# Порт прокси-сервера
PROXY_PORT=8080

# Имя пользователя для прокси (если требуется)
PROXY_USERNAME=your_proxy_username

# Пароль для прокси (если требуется)
PROXY_PASSWORD=your_proxy_password

# Тип прокси (http, https, socks4, socks5)
PROXY_TYPE=http

# ===========================================
# НАСТРОЙКИ EMAIL (для уведомлений)
# ===========================================
# SMTP сервер для отправки email
SMTP_SERVER=smtp.gmail.com

# Порт SMTP сервера (обычно 587 для TLS или 465 для SSL)
SMTP_PORT=587

# Имя пользователя для SMTP (обычно email адрес)
SMTP_USER=your_email@gmail.com

# Пароль для SMTP (или app password для Gmail)
SMTP_PASSWORD=your_email_password

# ===========================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
# ===========================================
# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Режим работы Chrome (headless=true для работы без окна браузера)
CHROME_HEADLESS=false

# Секретный ключ для сессий (обязательно для production)
SESSION_SECRET_KEY=your_secret_key_here
