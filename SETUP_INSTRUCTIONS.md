# Инструкция по настройке системы

## ✅ Текущий статус

### Настроено автоматически:
- ✅ **URL_PREFIX=/egoscan** - система будет доступна по адресу `http://test.pbd.space/egoscan`

### Требует настройки (опционально):

## 📧 Настройка SMTP для email уведомлений

Если вы хотите получать email уведомления о завершении парсинга, настройте SMTP в файле `.env`:

### Для Gmail:
```env
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
```
⚠️ **Важно для Gmail**: Используйте [App Password](https://support.google.com/accounts/answer/185833), а не обычный пароль. Включите 2FA и создайте App Password в настройках Google Account.

### Для Yandex:
```env
SMTP_SERVER=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your_email@yandex.ru
SMTP_PASSWORD=your_password
```

### Для Mail.ru:
```env
SMTP_SERVER=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=your_email@mail.ru
SMTP_PASSWORD=your_password
```

После настройки SMTP:
1. Раскомментируйте соответствующие строки в `.env`
2. Заполните свои данные
3. Перезапустите сервер

## 🔐 Настройка пароля

Пароль для доступа к веб-интерфейсу можно задать двумя способами:

### Способ 1: Через переменную окружения (рекомендуется)
Добавьте в `.env`:
```env
SITE_PASSWORD=your_secure_password
```

### Способ 2: Через config.json
Отредактируйте `config/config.json`:
```json
{
  "app": {
    "password": "your_secure_password"
  }
}
```

## 🌐 Настройка URL префикса

URL префикс уже настроен: `URL_PREFIX=/egoscan`

Система будет доступна по адресу: `http://test.pbd.space/egoscan`

Если нужно изменить префикс, отредактируйте `.env`:
```env
URL_PREFIX=/your_prefix
```

## 🔄 Перезапуск сервера

После изменения настроек в `.env` или `config.json` необходимо перезапустить сервер:

1. Остановите текущий сервер (Ctrl+C)
2. Запустите заново:
   ```bash
   python run_server.py
   ```

## ✅ Проверка настроек

После перезапуска сервера проверьте:

1. **Доступность по URL**: Откройте `http://localhost:8000/egoscan` (или ваш домен)
2. **Авторизация**: Введите пароль для входа
3. **Логи**: Проверьте файл `logs/parser.log` на наличие ошибок

## 📝 Примечания

- Файл `.env` содержит секретные данные и не должен попадать в git (уже в `.gitignore`)
- Все настройки из `.env` имеют приоритет над `config.json`
- URL_PREFIX работает автоматически через reverse-proxy или напрямую


